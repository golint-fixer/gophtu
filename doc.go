// Package gophtu package is set of simple test utils for Go.
// It contains: asserts, to simplify handling assertions in tests;
// timeouts handling, to make possible to preconfigure timeouts on different
// environments, where valid timeout might depend on testing environment's
// performance.
package gophtu
